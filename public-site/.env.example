# Public site (Next.js) environment
# Copy to .env and adjust for your local setup.
# Base URL of the backend API (used server-side for data fetching).
NEXT_PUBLIC_API_URL=http://localhost:4000/api/v1
