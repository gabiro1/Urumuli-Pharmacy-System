-- ============================================================
-- AUDIT ENUM EXTENSION
-- ============================================================
ALTER TYPE audit_entity ADD VALUE IF NOT EXISTS 'SETTINGS';
